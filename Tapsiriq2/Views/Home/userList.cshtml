@model List<User> 
    <h1>User List</h1><hr />
    <a asp-action="creatUser" asp-controller="home" class="btn btn-primary">Create User</a>
    <hr />
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>FirstName</th>
                <th>LastName</th>
                <th>DateOfBirth</th>
                <th>Country</th>
                <th>City</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.FirstName</td>
                    <td>@user.LastName</td>
                    <td>@user.DateOfBirth</td>
                    <td>@user.Country</td>
                    <td>@user.City</td>
                </tr>
            }
        </tbody>
    </table>